<div style="max-width:800px;margin:60px auto;padding:0 40px;">
  <div class="eyebrow" style="justify-content:flex-start;">{{ page.title }}</div>
  <h1 class="sec-title" style="text-align:left;margin-bottom:32px;">{{ page.title }}</h1>
  <div style="font-size:0.95rem;color:var(--text-mid);line-height:1.9;">
    {{ page.content }}
  </div>
</div>
